<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notesv2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/simply_notes"
        android:label="Simply Notes"
        android:roundIcon="@mipmap/simply_notes_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.samdroid.notesv2.CreateEditNotesActivity"
            android:parentActivityName="com.samdroid.notesv2.MainActivity"/>

        <activity
            android:name="com.samdroid.notesv2.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>